/**
 * Provides a functionality to convert points.
 * @see http://clubs.ya.ru/mapsapi/replies.xml?parent_id=6685&item_no=6654&with_parent=1#reply-mapsapi-6685
 * @static
 */
Crystal.Utils.Converter = {}

/**
 * Converts a pixel to geographic point.
 * @static
 * @param {Crystal.Pixel} pixel Pixel instance.
 * @return {Crystal.GeoPoint} Geographic point.
 */
Crystal.Utils.Converter.pixelToGeoPoint = function(pixel)
{
    
}

/**
 * Converts a geographic point to pixel.
 * @static
 * @param {Crystal.GeoPoint} geoPoint Geographic point.
 * @return {Crystal.Pixel} Pixel instance.
 */
Crystal.Utils.Converter.geoPointToPixel = function(geoPoint)
{
    
}